{
  "name": "@vane.build/sidecar",
  "version": "0.1.0",
  "description": "Zero-config sidecar proxy that attaches Counsel passports to outbound calls and verifies inbound credentials",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@peculiar/x509": "^1.14.3"
  }
}
